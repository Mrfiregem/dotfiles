#!/usr/bin/env bash
set -euo pipefail

# Export all variables automatically
set -a
# Call systemd generator that reads from environment.d files
source /dev/stdin <<EOF
$(/usr/lib/systemd/user-environment-generators/30-systemd-environment-d-generator)
EOF
set +a
